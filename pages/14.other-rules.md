---
layout: default
transition: none
---

<style scoped>
.slidev-vclick-hidden {
  display: none;
}
</style>

<section-title title="その他のルール" />

<div class="_bullet">

- Construct Props の各プロパティを自動で `readonly` にする

````md magic-move {at: 1}

```ts
import { IBucket } from "aws-cdk-lib/aws-s3";

interface MyConstructProps {
  bucket: IBucket; // ❌ 
}
```

```ts
import { IBucket } from "aws-cdk-lib/aws-s3";

interface MyConstructProps {
  readonly bucket: IBucket; // ✅ 
}
```

````

- Construct ID を自動で PascalCase にする

````md magic-move {at: 2}

```ts
import { Bucket } from "aws-cdk-lib/aws-s3";

const bucket = new Bucket(this, "my-bucket"); // ❌
```

```ts
import { Bucket } from "aws-cdk-lib/aws-s3";

const bucket = new Bucket(this, "MyBucket"); // ✅ 
```

````

</div>

<!-- 
他にも、[click]「Construct Props の各プロパティは自動で readonly にしてくれたり」、
[click] 「Construct ID を自動で PascalCase にしてくれたり」といった機能があり、特にまだ CDK 開発や TypeScript に慣れていないエンジニアにとっての、CDK コードの品質を支えるためのツールとして活用できます

また、この Plugin は型システムを利用してコードを検査していて、例えば 「Construct ID を PascalCase にする」というルールは、Construct 型ではないクラスには適用されない。といった感じで、厳密な判定が行われているのも、嬉しいポイントです。
-->