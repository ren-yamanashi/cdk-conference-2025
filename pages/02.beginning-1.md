---
layout: default
transition: none
---

<style scoped>
.slidev-vclick-hidden {
  display: none;
}

.small-code {
  .slidev-code {
    font-size: 0.98rem !important;
    line-height: 0rem !important;
  }
}

.slidev-layout {
  padding-left: 0.5rem !important;
  padding-right: 0.5rem !important;
}

.absolute-text {
  position: absolute;
  color: red;
  background-color: white;
  padding: 5px;
  text-align: center;
  align-items: center;
}

.text-item-1 {
  top: 65px;
  left: 400px;
}

.text-item-2 {
  top: 230px;
  left: 300px;
}

.text-item-3 {
  top: 350px;
  left: 500px;
}

</style>

<div class="custom-layout mt-2 flex justify-around small-code" v-click="[0]">

```ts{*}
interface MyConstructProps {
  readonly certificate: ICertificate;
  readonly domainName: string;
}

export class MyConstruct extends Construct {
  constructor(
    scope: Construct,
    id: string,
    props: MyConstructProps
  ) {
    super(scope, id);

    new Distribution(this, "Distribution", {
      certificate: props.certificate,
      domainNames: [props.domainName],
      // ...
    });
  }
}
```

<div class="small-code">

```ts{*}
interface Args {
  certificate: Certificate;
  readonly domainName: string;
}

export class MyConstruct extends Construct {
  constructor(
    construct: Construct,
    name: string,
    args: Args
  ) {
    super(construct, name);

    new Distribution(this, Distribution.name, {
      certificate: args.certificate,
      domainNames: [args.domainName],
      // ...
    });
  }
}
```

</div>

</div>

<div v-click="1">

<div class="custom-layout mt-2 flex justify-around small-code">

```ts{2,8-10,14}
interface MyConstructProps {
  readonly certificate: ICertificate;
  readonly domainName: string;
}

export class MyConstruct extends Construct {
  constructor(
    scope: Construct,
    id: string,
    props: MyConstructProps
  ) {
    super(scope, id);

    new Distribution(this, "Distribution", {
      certificate: props.certificate,
      domainNames: [props.domainName],
      // ...
    });
  }
}
```

<div class="small-code">

```ts{5}
interface Args {
  certificate: Certificate;
  readonly domainName: string;
}

export class MyConstruct extends Construct {
  constructor(
    construct: Construct,
    name: string,
    args: Args
  ) {
    super(construct, name);

    new Distribution(this, Distribution.name, {
      certificate: args.certificate,
      domainNames: [args.domainName],
      // ...
    });
  }
}
```

</div>

</div>

<div class="absolute-text text-item-1 mt-2">

<span>
⭕️ props のプロパティが読み取り専用になっている
</span>

</div>

<div class="absolute-text text-item-2 mt-2">

<span>
⭕️ constructor のプロパティの命名の意味が明瞭
</span>

</div>

<div class="absolute-text text-item-3 mt-2">

<span>
⭕️ Construct ID に変数を使用していない
</span>

</div>

</div>

<!--
早速ですが、こちらの二つのコードをご覧ください。この二つは、ハッシュ値などを除いて、最終的には同じ CloudFormation Template になりますが、どちらのコードが良いコードでしょうか？  

[click] 私は、左の方が良いコードである。と評価しました。
-->