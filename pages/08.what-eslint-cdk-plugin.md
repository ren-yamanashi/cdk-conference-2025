---
layout: default
transition: none
---

<style scoped>
.slidev-vclick-hidden {
  display: none;
}

.medium-code {
  .slidev-code {
    font-size: 1.2rem !important;
    line-height: 0rem !important;
  }
}

.small-code {
  .slidev-code {
    font-size: 0.8rem !important;
    line-height: 0rem !important;
  }
}

.image {
  border-radius: 5px;
  box-shadow: 0px 10px 15px 0px gray;
  margin: 40px auto auto auto;
}
</style>

<section-title title="CDK 用の ESLint Plugin" />

<div class="medium-code" v-click="[0]">

#### インストール

```bash
pnpm install -D eslint-cdk-plugin
```

<br />

#### 設定

```ts
// eslint.config.mjs
import cdkPlugin from 'eslint-cdk-plugin';
import tseslint from 'typescript-eslint';

export default tseslint.config(
  ...tseslint.configs.recommended,
  {
    files: ['lib/**/*.ts', 'bin/*.ts'],
    extends: [cdkPlugin.configs.recommended], // ✅ 一行追加するだけ
    // ...
```

</div>

<div class="small-code" v-click="[1]">

#### インストール

```bash
pnpm install -D eslint typescript-eslint eslint-cdk-plugin
```

<br />

#### 設定

```ts
// eslint.config.mjs
import eslint from "@eslint/js";
import tseslint from 'typescript-eslint';
import cdkPlugin from 'eslint-cdk-plugin';

export default tseslint.config(
  eslint.configs.recommended,
  ...tseslint.configs.recommended,
  {
    files: ['lib/**/*.ts', 'bin/*.ts'],
    extends: [cdkPlugin.configs.recommended],
  },
  {
    ignores: ['node_modules/', 'cdk.out/', '*.js'],
  }
);
```

</div>

<div v-click="2" class="_bullet">

- CDK に ESLint を導入する
  - v8 は EOL (End Of Life) なので v9 を使おう！

<img src="/cdk-eslint-install.png" class="w-160 h-110 mx-10 image" alt="How to install eslint at cdk project" />

</div>

<!--
まず、利用方法についてですが、こちらは非常に簡単です。  
すでに eslint の バージョン9 を利用されている場合は、プラグインをインストールし、設定ファイル(eslint.config.mjs)のextendsプロパティを追加するだけです

ここでは pnpm のインストール方法のみ記載していますが、yarn や npm を使用したインストールも可能です

[click] もし eslint をまだ利用していない場合は、こちらのコマンドを実行して eslint やその周辺ライブラリをインストールし、こちらに記載されたコードを eslint.config.mjs に貼り付けるだけです。  

[click] CDK に ESLint を導入する詳しい内容については、以前私が Zenn に記事を書いていますので、是非そちらをご覧ください！

ちなみに、eslint の バージョン8以下はすでに End of Life を過ぎているので、バージョン8以下を使用されている方は、お早めにバージョン9に移行することをお勧めします！
-->
